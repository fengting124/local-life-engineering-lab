# ================================================================
# LocalLife Copilot Agent Service 依赖
# ================================================================

# ===== Web 框架（SSE 流式输出）=====
fastapi==0.115.5
uvicorn[standard]==0.32.1

# ===== LangGraph Agent 编排 =====
langgraph==0.2.45
langchain==0.3.7
langchain-anthropic==0.3.0
langchain-openai==0.2.6

# ===== LLM =====
anthropic==0.40.0

# ===== RAG 完整流水线 =====
pymilvus==2.4.9
langchain-milvus==0.1.6
# marshmallow 必须 <4：4.x 移除了 __version_info__，与 pymilvus 2.4.9 的 environs 依赖不兼容
# 否则报错 "module 'marshmallow' has no attribute '__version_info__'"，Milvus 整个不可用
marshmallow<4
rank-bm25==0.2.2
numpy>=1.26.0

# sentence-transformers + torch（本地 Embedding + Reranker）
# 注意：torch CPU 版约 700MB，首次安装较慢
# 如果只用 Milvus 远程服务而不需要本地 embedding，可以注释掉这两行
sentence-transformers==3.3.1
# torch 使用 CPU 轻量版（无 GPU 时 ~700MB，比 CUDA 版 ~2.5GB 小很多）
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# ===== 数据库（会话 + 消息 + checkpoint 持久化）=====
sqlalchemy==2.0.36
aiomysql==0.2.0
alembic==1.14.0

# ===== Redis（限流 + 缓存）=====
redis==5.2.0

# ===== HTTP 客户端（调用 Java MCP Server）=====
httpx==0.28.0

# ===== 数据校验 =====
pydantic==2.10.1
pydantic-settings==2.6.1

# ===== 异步工具 =====
anyio==4.7.0

# ===== 可观测性 =====
prometheus-fastapi-instrumentator==7.0.0
prometheus-client==0.21.1

# ===== 日志 =====
structlog==24.4.0

# ===== 开发工具 =====
python-dotenv==1.0.1
