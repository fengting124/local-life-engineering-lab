apiVersion: 1

providers:
  - name: local-life-observability
    orgId: 1
    folder: LocalLife
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
