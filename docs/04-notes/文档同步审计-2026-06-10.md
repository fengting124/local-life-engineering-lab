# 文档同步审计（2026-06-10）

本文记录当前文档与代码/运行状态的一致性检查结果，方便后续维护时判断哪些文档是最新入口，哪些只是历史快照。

## 当前真实状态

| 项 | 当前状态 |
| --- | --- |
| Java 主服务 | `local-life-server`，端口 8080，镜像 `locallife-server:latest` |
| Java MCP 服务 | `local-life-copilot`，端口 8081，镜像 `locallife-copilot:latest` |
| Python Agent 服务 | `copilot-agent-service`，端口 8000，镜像 `copilot-agent:latest` |
| 默认 LLM | `deepseek-v4-flash`，通过 `LLM_PROVIDER=deepseek` + `LLM_API_KEY` 配置 |
| Swagger/OpenAPI | Java: `/swagger-ui.html` + `/v3/api-docs`；Python Agent: `/docs` + `/openapi.json`，并兼容 `/swagger-ui.html` + `/v3/api-docs` |
| 批量演示数据 | `scripts/business-bulk-simulate.sh`，默认 2000 用户、50 商家、100 门店、100 券、10000 订单、约 2% 异常 |
| 测试数字 | server 79；copilot 76；agent 187（Agent 测试收集需 `DEBUG=false`） |

## 已同步的主入口文档

- `README.md`
- `docs/README.md`
- `docs/01-project/00-文档索引.md`
- `docs/04-notes/Swagger在线接口文档.md`
- `docs/demo/面试演示脚本.md`
- `local-life-copilot/README.md`
- `copilot-agent-service/README.md`

## 仍可能看到旧说法的文档

这些文件主要是历史交接、调研记录或设计演进过程，不建议当作最新运行手册直接执行：

- `docs/00-handoff/WSL_RAG_MODEL_DEPLOYMENT_HANDOFF.md`
- `docs/04-notes/WSL迁移交接文档.md`
- `docs/04-notes/交接提示词_继续工作.md`
- `docs/04-notes/核心目标与工作指令.md`
- `docs/04-notes/调研记录与优化规划.md`
- `docs/04-notes/5.26.md`
- `docs/04-notes/5.27.md`
- `docs/04-notes/个人学习笔记 2026.5.25.md`

这些文档里出现 Claude、Anthropic、旧启动方式、旧 TODO 或阶段性结论时，应理解为历史上下文。最新操作以主入口文档和 `.env.example` 为准。

## 注意事项

- `query_order` 当前 schema 参数名为 `order_id`，但实现按业务订单号 `order_no` 查询；演示脚本和 MCP README 已显式说明该历史命名不一致。
- Agent 测试在本地收集/运行时建议显式加 `DEBUG=false`，避免 shell 环境变量污染 Pydantic Settings。
- 批量模拟数据是 synthetic data，仅用于本地/预发演示和压测，不应作为生产初始化数据。
